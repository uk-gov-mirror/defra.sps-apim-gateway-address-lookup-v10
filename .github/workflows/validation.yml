name: Validate APIM Structure

on:
  pull_request:
    branches:
      - master
      - develop

jobs:
  validate-apim-structure:
    runs-on: windows-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Run APIM Validation check
        shell: pwsh
        run: |
          $RootPath = "${{ github.workspace }}"
          $ParentFolders = @("external", "internal")
          $Environments = @("base")

          # Execute external script
          .\scripts\Validate-APIMStructure.ps1 -RootPath $RootPath -ParentFolders $ParentFolders -Environments $Environments
